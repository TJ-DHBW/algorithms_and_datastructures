package org.dhbw.algorithms;

import org.dhbw.NotYetImplementedException;

public class LinearSearch implements SearchStrategy {
    /**
     * Searches through an array by comparing every value to the target.
     *
     * @param arrayToSearchIn The array in which to search.
     * @param target          The target value to search for.
     * @return The index of the target in the array, or 0 if the target is not in the array.
     */
    @Override
    public int search(int[] arrayToSearchIn, int target) {
        throw new NotYetImplementedException();
    }
}
